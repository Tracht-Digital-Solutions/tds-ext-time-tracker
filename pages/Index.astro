---
// The /time page, injected into the host build via the contract's route slot.
// In the real extension this hosts the full TimeTracker island; the smoke keeps
// it minimal to validate route injection from a package.
import WeekSummary from "../islands/WeekSummary.tsx";
---

<section class="page page--time">
  <h1>Zeiterfassung</h1>
  <WeekSummary client:load />
</section>
